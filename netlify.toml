[build]
  base = "Client"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # Set VITE_API_BASE_URL in Netlify UI (not here — avoids secret-scanner build failures)

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
